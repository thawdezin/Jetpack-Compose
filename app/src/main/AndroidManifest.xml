<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thawdezin.violet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LayoutTestingActivity"></activity>
        <activity android:name=".ScrollerOnDataChange" />
        <activity android:name=".MultiViewWithoutFragment" />
        <activity android:name=".SplashScreenActivity" />
        <activity android:name=".BottomNavigationActivity" />
        <activity android:name=".TextChangedOnButtonClickActivity" />
        <activity android:name=".TwoEqualContainerActivity" />
        <activity android:name=".FragmentContainerActivity" />
        <activity android:name=".DateTimeActivity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".BottomNavigation" />
        <activity android:name=".NestedScrollActivity" />
        <activity android:name=".StackLayoutActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>